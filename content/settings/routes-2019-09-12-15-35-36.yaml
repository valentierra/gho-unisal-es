routes:
  /canal/:
    controller: channel
    filter: tag:[canal]
    data:
      post: page.canal
    order: published_at desc
    template: canal

collections:
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
